{
  "name": "frontbase-framework",
  "private": true,
  "description": "The Frontbase framework monorepo — universal SSR engine, compiler, components, and edge infrastructure",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --filter=!@frontbase/console test",
    "check": "pnpm -r check",
    "test:mutation": "pnpm -r test:mutation",
    "deploy:cf-full": "node scripts/deploy.mjs",
    "deploy:vercel": "node scripts/deploy-vercel.mjs",
    "deploy:deno": "node scripts/deploy-deno.mjs",
    "contracts:embed": "node scripts/embed-community-spec.mjs",
    "console:build": "node scripts/build-console.mjs",
    "console:stage": "node scripts/build-console.mjs --skip-build",
    "console:check": "node scripts/validate-console-pin.mjs"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3"
  },
  "pnpm": {
    "//": "Fidelity pins: the console SPA must build byte-comparably to the product build it replaces. The console's own devDependencies are exact-pinned; these overrides hold the only remaining resolution drift that reaches its output — the 7 runtime libs bundled into the SPA (floating-ui stack via radix popper, scroll-lock pair, monaco loader) and rollup, vite's chunk codegen, whose patch releases change emitted bytes.",
    "overrides": {
      "@floating-ui/core": "1.7.2",
      "@floating-ui/dom": "1.7.2",
      "@floating-ui/react-dom": "2.1.4",
      "@floating-ui/utils": "0.2.10",
      "@monaco-editor/loader": "1.5.0",
      "aria-hidden": "1.2.4",
      "react-remove-scroll": "2.7.1",
      "rollup": "4.59.0"
    }
  }
}
