{
  "name": "frontbase-framework",
  "private": true,
  "description": "The Frontbase framework monorepo — Chimera (Universal eSSR) engine, compiler, components, and edge infrastructure",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check": "pnpm -r check",
    "test:mutation": "pnpm -r test:mutation",
    "deploy:cf-full": "node scripts/deploy.mjs",
    "contracts:sync": "node scripts/sync-contract.mjs",
    "contracts:diff": "node scripts/contract-diff.mjs",
    "test:contract-drift": "node scripts/test-contract-drift.mjs",
    "fetch:console": "node scripts/fetch-console.mjs",
    "console:check": "node scripts/validate-console-pin.mjs"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3"
  }
}
