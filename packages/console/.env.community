# Community / self-host edition build (CF-22 P0 / W3).
# Loaded by `npm run build:community` (vite build --mode community).
# Pins the edition explicitly so the community bundle is reproducible even if
# the ambient shell exports VITE_DEPLOYMENT_MODE=cloud.
VITE_DEPLOYMENT_MODE=self-host
