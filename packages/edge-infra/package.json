{
  "name": "@frontbase/edge-infra",
  "version": "0.0.0",
  "description": "Frontbase edge infrastructure — concrete DataProviders, Edge Data Proxy auth, cache, queue, vault (Web Crypto), AI/MCP executors. SERVER-ONLY (never browser-imported).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node test/providers.mjs && node test/isolation.mjs && node test/proxy-auth.mjs && node test/vault.mjs && node test/cache.mjs && node test/workflow-durable.mjs && node test/no-leak.mjs",
    "test:mutation": "node test/mutation.mjs"
  },
  "peerDependencies": {
    "@frontbase/edge-core": "workspace:*"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4"
  }
}