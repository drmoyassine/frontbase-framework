{
  "name": "@frontbase/edge-infra",
  "version": "0.0.0",
  "description": "Frontbase edge infrastructure — concrete DataProviders, Edge Data Proxy auth, cache, queue, vault (Web Crypto), AI/MCP executors. SERVER-ONLY (never browser-imported).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node test/password.mjs && node test/session.mjs && node test/runners.mjs && node test/supabase-rpc-parse.mjs && node test/providers.mjs && node test/isolation.mjs && node test/proxy-auth.mjs && node test/vault.mjs && node test/cache.mjs && node test/workflow-durable.mjs && node test/ratelimit.mjs && node test/no-leak.mjs",
    "test:mutation": "node test/mutation.mjs"
  },
  "peerDependencies": {
    "@frontbase/edge-core": "workspace:*"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1085.0",
    "@aws-sdk/s3-request-presigner": "^3.1085.0",
    "@libsql/client": "^0.17.4",
    "@neondatabase/serverless": "^1.1.0",
    "@supabase/postgrest-js": "^2.110.2"
  }
}