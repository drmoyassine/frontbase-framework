{
  "name": "@frontbase/ui-components",
  "version": "0.0.0",
  "description": "The single set of isomorphic page components (no React) + behaviors + auth primitives",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
